[build]
builder = "DOCKERFILE"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# GET / returns 200 -- see server.js's health branch.
healthcheckPath = "/"
healthcheckTimeout = 10
